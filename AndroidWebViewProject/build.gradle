buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.0' // Use a recent stable version
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin:1.8.0' // Use a compatible Kotlin version
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
